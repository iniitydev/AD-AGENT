{
  "name": "AD-AGENT",
  "description": "An LLM-driven multi-agent anomaly detection platform.",
  "variables": [
    {
      "name": "OPENAI_API_KEY",
      "description": "Enter your OpenAI API key (optional, press Enter to skip and use a local LLM)",
      "type": "string",
      "default": ""
    }
  ],
  "setup": [
    "pip install -r requirements.txt"
  ],
  "run": {
    "default": "python main.py",
    "parallel": "python main.py -p",
    "optimizer": "python main.py -o"
  }
}
