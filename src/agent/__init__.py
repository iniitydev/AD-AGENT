# src/agent/__init__.py
# This file makes Python treat the `agent` directory as a sub-package of `src`.

# Optionally, you can define what gets imported when someone does `from src.agent import *`
# For example:
# from .detection import train_model, predict_data
# from .preprocessing import preprocess_data
# from .encryption import encrypt_data, decrypt_data
# from .data_manifest import create_manifest

# For now, keeping it simple.
